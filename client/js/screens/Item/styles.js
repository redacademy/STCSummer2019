import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  carouselContainer: {
    marginLeft: '10%',
    marginRight: '10%',
    marginTop: '3%',
  },
});

export default styles;
