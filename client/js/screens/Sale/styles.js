import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  allItems: {
    flexDirection: 'row',
    flexWrap: 'wrap',
    paddingTop: '3%',
  },
  container: {
    paddingTop: '3%',
  },
});

export default styles;
